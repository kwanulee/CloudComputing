# Amazon EC2 개요
- EC2 란?
- EC2 주요 개념 및 기능
- EC2 실습

## 1. Elastic Compute Cloud (EC2)란?
EC2는 Amazon Elastic Compute Cloud의 약자로, **AWS(Amazon Web Services)**에서 제공하는 가상 서버 서비스입니다. EC2는 클라우드에서 가상화된 서버를 쉽게 생성하고 관리할 수 있게 해주며, 사용자는 필요에 따라 서버의 크기, 저장 공간, 네트워크 대역폭 등을 조정할 수 있습니다.

EC2의 주요 특징은 다음과 같습니다:
- 탄력성: 사용자는 필요한 만큼 서버를 생성하고, 사용하지 않을 때는 중지하거나 삭제할 수 있습니다.
- 확장성: 서버 리소스를 쉽게 확장하거나 축소할 수 있어, 트래픽 변화에 유연하게 대응할 수 있습니다.
- 비용 효율성: 사용한 만큼만 비용을 지불하는 방식으로, 초기 하드웨어 투자 없이 클라우드 컴퓨팅을 이용할 수 있습니다.
- 다양한 인스턴스 타입: CPU, 메모리, 저장소 등의 요구 사항에 맞게 다양한 유형의 인스턴스를 선택할 수 있습니다.

이 서비스를 통해 기업과 개발자들은 애플리케이션을 쉽게 배포하고, 테스트하며, 운영할 수 있습니다.

## 2. EC2 주요 개념 및 기능
- [인스턴스, 인스턴스 유형](#instance)
- [인스턴스 요금](#price)
- [EC2의 스토리지](#storage)
- [보안그룹](#sg)
- [인스턴스 로그인 정보 보호](#login)
- [인스턴스 생명주기](#lifecycle)
- [아마존 머신 이미지 (AMI)](#ami)

<a name="instance"></a> 
### 2.1 인스턴스, 인스턴스 유형
- **인스턴스**: 가상 컴퓨팅 환경
- **인스턴스 유형 (Instance type)** : 인스턴스에 사용되는 호스트 컴퓨터의 사양 (CPU, 메모리, 스토리지, 네트워크 대역)을 정의한 것

#### 2.1.1 인스턴스 유형
인스턴스에서 실행하려는 애플리케이션에 따라 [인스턴스 유형](https://aws.amazon.com/ko/ec2/instance-types/) 선택
- **범용 인스턴스** : 범용 인스턴스는 균형 잡힌 CPU, 메모리, 네트워크 성능을 제공하여 다양한 애플리케이션에서 사용할 수 있습니다. 비용과 성능의 균형을 맞추고자 할 때 적합합니다.
	- t4g, t3, t3a, t2 (Burstable): 가벼운 애플리케이션에 적합하며, 부하가 일시적으로 증가하는 경우에도 대응할 수 있습니다. 기본 성능을 초과하는 경우 버스트 크레딧을 사용하여 처리량을 증가시킬 수 있습니다.
		- 사용 사례: 개발 및 테스트 환경, 저트래픽 웹 서버, 작은 데이터베이스 등.
	- m7g, m6g, m5, m4 (Balanced Performance): CPU, 메모리, 네트워크 성능의 균형을 이루며, 대부분의 일반적인 애플리케이션에 적합합니다.
		- 사용 사례: 중간 규모의 데이터베이스, 웹 애플리케이션, 엔터프라이즈 애플리케이션.
- **컴퓨팅 최적화 인스턴스** : 컴퓨팅 성능이 중요한 워크로드에 적합합니다. CPU 집약적인 애플리케이션에서 높은 성능을 발휘합니다.
	- c7g, c6g, c5, c4: 고성능 프로세서를 제공하며, CPU 성능이 중요한 작업에 적합합니다.
		- 사용 사례: 고성능 컴퓨팅(HPC), 게임 서버, 웹 서버, 배치 처리, 동영상 인코딩 등.
- **메모리 최적화 인스턴스**: 메메모리가 많은 애플리케이션에 최적화된 인스턴스입니다. 대규모 데이터 세트를 처리하거나 메모리 사용이 많은 애플리케이션에 적합합니다.
	- r7g, r6g, r5, r4: 메모리 용량이 높아 대규모 데이터베이스나 인메모리 애플리케이션에 적합합니다.
		- 사용 사례: 고성능 데이터베이스, 인메모리 캐시(Redis, Memcached), 실시간 빅데이터 분석.
	- x2idn, x2iedn: 매우 높은 메모리 비율을 제공하며, 대규모 인메모리 데이터베이스 및 분석 시스템에 적합합니다.
		- 사용 사례: SAP HANA, Oracle, Microsoft SQL Server 같은 대규모 엔터프라이즈 데이터베이스.
- **가속화된 컴퓨팅 인스턴스**: GPU나 FPGA 같은 하드웨어 가속 기능을 제공하여, 그래픽 처리 또는 머신 러닝 모델 학습처럼 컴퓨팅 성능이 중요한 작업에 적합합니다.
	- p5, p4, p3 (GPU Optimized): NVIDIA GPU가 탑재되어 고성능 머신 러닝 모델 훈련, 과학적 시뮬레이션, 그래픽 렌더링에 적합합니다.
		- 사용 사례: 딥러닝, 고성능 그래픽 처리, AI 연구, 3D 애니메이션.
	- g5, g4ad, g4dn: NVIDIA GPU를 활용하여 머신 러닝 추론 및 그래픽 집약적 애플리케이션에 적합한 인스턴스입니다.
		- 사용 사례: 실시간 스트리밍, 게임, 머신 러닝 추론, 그래픽 디자인, 비디오 트랜스코딩.
	- f1 (FPGA Optimized): FPGA(Field Programmable Gate Arrays)를 사용하여 매우 특정한 연산 집약적인 작업을 가속화합니다.
		- 사용 사례: 금융 리스크 분석, 유전자 시퀀싱, 데이터 암호화.
- **스토리지 최적화 인스턴스**: 고속 I/O 성능이 중요한 애플리케이션에 적합하며, SSD 기반의 높은 디스크 처리량을 제공하는 인스턴스입니다.
	- i4i, i3: NVMe SSD 스토리지를 사용하여 매우 높은 IOPS(입출력 작업 속도)를 제공하며, 데이터베이스와 같은 스토리지 집약적인 애플리케이션에 적합합니다.
		- 사용 사례: NoSQL 데이터베이스(Cassandra, MongoDB), 데이터 웨어하우스, OLTP 시스템.
	- d2, d3, d3en: 대규모 데이터 저장이 필요한 애플리케이션에 최적화된 인스턴스로, 높은 처리량과 대용량 HDD 스토리지를 제공합니다.
		- 사용 사례: 대규모 데이터 처리 및 분석, 분산 파일 시스템, 로그 처리.
- **HPC 최적화 인스턴스** : HPC 인스턴스는 과학적 연구, 공학 시뮬레이션, 대규모 데이터 분석 등 매우 높은 성능을 요구하는 워크로드에 적합합니다.
	- hpc6a: 고성능 컴퓨팅 워크로드를 처리하는 데 최적화되어 있으며, CPU 성능과 네트워크 성능이 매우 높은 인스턴스입니다.
		- 사용 사례: 유체 역학 시뮬레이션, 분자 모델링, 대규모 병렬 처리 애플리케이션.

#### 2.1.2 인스턴스 유형 명명 규칙
AWS EC2 인스턴스 유형의 이름에는 일정한 명명 규칙이 존재하며, 이는 인스턴스의 특징과 목적을 명확히 나타내기 위해 사용됩니다. 인스턴스 이름은 다음과 같은 구조를 따릅니다:

```
<인스턴스 패밀리><세대><성능 지표>.<추가 기능>
```

1. 인스턴스 패밀리 (Instance Family)
	
	인스턴스 패밀리는 해당 인스턴스가 속한 카테고리를 나타냅니다. 인스턴스가 어떤 작업에 최적화되어 있는지를 보여주며, 아래와 같이 주요 패밀리가 있습니다:

	- t: 범용(General Purpose) 인스턴스 (버스트 가능 성능)
	- m: 범용(General Purpose) 인스턴스 (균형 잡힌 성능)
	- c: 컴퓨팅 최적화(Compute Optimized)
	- r: 메모리 최적화(Memory Optimized)
	- i: 스토리지 최적화(Storage Optimized)
	- g: 가속화된 컴퓨팅(GPU) 인스턴스
	- p: GPU를 사용한 고성능 컴퓨팅 (ML 및 HPC)
	- x: 고성능 메모리 (Memory Optimized, 초대형 메모리)
	- hpc: 고성능 컴퓨팅 (HPC) 최적화
	- f: FPGA(재구성 가능한 하드웨어) 인스턴스

2. 세대 (Generation)
	
	인스턴스의 세대를 나타내며, 기술적 발전에 따라 새로운 세대가 나오면 숫자가 증가합니다. 예를 들어 m6는 6세대 m 인스턴스를 의미합니다. 세대가 증가할수록 최신 기술을 기반으로 더 높은 성능과 효율성을 제공합니다.

	- 예: t3는 t 시리즈의 3세대, c6g는 c 시리즈의 6세대 인스턴스입니다.

3. 성능 지표 (Performance Metric)
	
	성능 지표는 해당 인스턴스의 추가적인 성능 특성을 설명합니다. 주로 a, g, i, n, d 등의 문자가 붙으며, 각각의 문자는 특정한 기술이나 성능을 나타냅니다.

	- a: AMD 기반 프로세서를 사용함 (예: m5a, t3a)
	- g: Arm 기반 프로세서 (Graviton) 사용 (예: c6g, m6g)
	- i: 고성능 스토리지 인스턴스 (예: i3, i4i)
	- n: 네트워크 성능이 강화된 인스턴스 (예: m5n)
	- d: 인스턴스가 로컬 스토리지를 포함 (예: m5d, c5d)

	이 외에도 특정 기술이나 성능 요구 사항에 맞춘 지표가 붙을 수 있습니다.

4. 추가 기능 (Additional Features)
	
	추가 기능은 점이나 슬래시 뒤에 붙는 부분으로, 특정 하드웨어 구성을 설명하는 경우가 많습니다. 이 부분은 자주 등장하지 않지만, 인스턴스의 특수한 변형을 설명할 때 사용됩니다.
	- metal: 베어메탈(bare metal) 인스턴스, 가상화 계층이 없고 직접 하드웨어에 접근 가능 (예: m5.metal, c5.metal)
	- highmem: 대용량 메모리를 제공 (예: x1e.highmem)

	인스턴스 이름에는 크기(예: small, medium, large, xlarge)가 추가로 붙으며, 이는 인스턴스의 CPU, 메모리, 네트워크 성능 크기를 의미합니다. 예를 들어, xlarge는 large보다 더 많은 리소스를 할당받습니다. 크기는 주로 다음과 같은 형식을 따릅니다:

	- nano, micro, small, medium, large
	- xlarge, 2xlarge, 4xlarge, 8xlarge, 16xlarge, …

	인스턴스 크기는 CPU 코어 수, 메모리 크기 등에 따라 차이가 나며, 리소스가 많은 워크로드일수록 더 큰 크기의 인스턴스를 사용합니다.

예시로 살펴보는 인스턴스 이름 명명 규칙

- t3.micro:
	- t: 범용(General Purpose) 인스턴스
	- 3: 3세대
	- micro: 인스턴스 크기(적은 리소스)
- m5a.large:
	- m: 범용(General Purpose), 균형 잡힌 성능
	- 5: 5세대
	- a: AMD 기반 프로세서
	- large: 인스턴스 크기
- c6g.xlarge:
	- c: 컴퓨팅 최적화
	- 6: 6세대
	- g: Arm 기반 Graviton 프로세서 사용
	- xlarge: 인스턴스 크기
- i3en.metal:
	- i: 스토리지 최적화
	- 3: 3세대
	- en: 향상된 네트워크 성능 제공
	- metal: 베어메탈 인스턴스

이 명명 규칙을 통해 EC2 인스턴스가 어떤 성능 특성을 가지고 있는지 쉽게 파악할 수 있으며, 각 애플리케이션의 요구 사항에 맞는 적절한 인스턴스를 선택할 수 있습니다.

---
<a name="price"></a> 
### 2.2 인스턴스 요금
EC2 인스턴스 요금은 크게 다음과 같은 결제 방식으로 나눌 수 있습니다:

1) 온디맨드 인스턴스 (On-Demand Instances)

	온디맨드 인스턴스는 사용한 만큼만 비용을 지불하는 결제 방식입니다. 미리 예약하거나 장기 약정을 하지 않아도 되며, 인스턴스를 실행한 시간 동안만 과금됩니다. 유연성이 높고, 예측 불가능한 트래픽이나 일시적인 워크로드에 적합합니다.

	- 특징:
		- 미리 예약할 필요 없음.
		- 인스턴스 종료 시 즉시 중지.
		- 시간 단위로 과금 (초 단위로 과금이 시작됨).
	- 사용 사례:
		- 단기적으로 필요하거나, 예측이 어려운 트래픽 변화가 있는 애플리케이션.
		- 애플리케이션 개발 및 테스트 환경.

2) 예약 인스턴스 (Reserved Instances, RI)

	예약 인스턴스는 1년 또는 3년 단위로 EC2 인스턴스를 예약하여 할인을 받는 방식입니다. 온디맨드 요금보다 최대 72%까지 할인된 가격을 제공합니다. 특정 애플리케이션이 장기간 일정한 컴퓨팅 자원을 요구하는 경우 적합합니다.

	- 특징:
		- 1년 또는 3년 계약.
		- 최대 72%까지 할인을 제공.
		- 가용영역 또는 특정 인스턴스 유형에 대해 예약 가능.
		- 전체 선결제(All Upfront), 부분 선결제(Partial Upfront), 무선결제(No Upfront) 옵션 중 선택 가능.
	- 사용 사례:
		- 장기적으로 예측 가능한 워크로드.
		- 24시간 구동되는 웹 애플리케이션, 데이터베이스 서버 등.

3) 스팟 인스턴스 (Spot Instances)

	스팟 인스턴스는 AWS에서 남은 유휴 컴퓨팅 자원을 경매 방식으로 저렴하게 제공하는 인스턴스 유형입니다. 스팟 인스턴스는 온디맨드 인스턴스보다 최대 90%까지 저렴하지만, AWS에서 해당 리소스를 필요로 할 경우 인스턴스가 중단될 수 있습니다.

	- 특징:
		- 매우 저렴한 가격(최대 90% 할인).
		- 언제든지 중단될 수 있음(2분 전 알림 제공).
		- 잦은 중단이 허용되는 워크로드에 적합.
	- 사용 사례:
		- 배치 작업, 테스트 환경.
		- 주로 빅데이터 처리, ML 등 많은 인스턴스가 필요한 작업에 사용
		- 장기 실행을 필요로 하지 않는 애플리케이션.

4) 전용 호스트 (Dedicated Hosts)

	전용 호스트는 물리적 서버 전체를 단독으로 사용할 수 있는 옵션입니다. 이를 통해 가상화 계층 없이 직접 하드웨어에 접근할 수 있으며, BYOL(Bring Your Own License, 자체 라이선스 사용)이 필요한 경우 유용합니다.

	- 특징:
		- 물리적 서버를 다른 고객과 공유하지 않음.
		- BYOL이 필요한 소프트웨어 환경에 적합.
			- Windows Server, SQL Server, SUSE Linux Enterprise Server(라이선스 약관에 따름)를 비롯한 기존 서버 한정 소프트웨어 라이선스를 사용할 수 있으므로 비용을 절감 가능
		- 기업 규정 준수 및 규제 요구 사항을 충족하는 인스턴스 배포 가능

	- 사용 사례:
		- 규제 준수(예, 보안)가 필요한 워크로드(예: 금융, 정부 관련).
		- 많은 소프트웨어 벤더들이 라이선스 정책을 물리적 하드웨어에 기반하여 적용하기 때문에, 전용 호스트는 이러한 소프트웨어를 사용하는 기업들이 라이선스 비용을 최적화하고 규정을 준수하는 데 중요한 역할을 합니다.


<a name="storage"></a> 
### 2.3 EC2의  스토리지
Amazon EC2의 스토리지는 EC2 인스턴스가 데이터를 저장하고 처리하는 데 필요한 다양한 스토리지 옵션을 제공합니다. 각 스토리지 옵션은 애플리케이션의 요구 사항에 따라 성능, 용량, 내구성 측면에서 차이가 있습니다. EC2의 주요 스토리지 옵션에는 Amazon EBS(Elastic Block Store), 인스턴스 스토어, Amazon EFS(Elastic File System), Amazon S3 (Simple Storage Service) 등이 있습니다. 다음은 각각의 스토리지에 대한 자세한 설명입니다.

![](https://docs.aws.amazon.com/images/AWSEC2/latest/UserGuide/images/architecture_storage.png)

#### 2.3.1 Amazon EBS (Elastic Block Store)

Amazon EBS는 EC2 인스턴스에 사용할 수 있는 블록 스토리지 서비스로서, *Amazon EBS 볼륨*과 *Amazon EBS 스냅샷*을 생성하고 관리할 수 있습니다.
- *Amazon EBS 볼륨* - Amazon EC2 인스턴스에 연결하는 스토리지 볼륨입니다. 볼륨을 인스턴스에 연결하면 해당 볼륨을 컴퓨터에 연결된 로컬 하드 드라이브처럼 사용할 수 있습니다(예: 파일 저장 또는 애플리케이션 설치).

- *Amazon EBS 스냅샷* - 볼륨 자체와 독립적으로 유지되는 Amazon EBS 볼륨의 point-in-time 백업입니다. 스냅샷을 생성하여 Amazon EBS 볼륨의 데이터를 백업할 수 있습니다. 이 스냅샷은 S3에 저장되며, 나중에 복구하거나 다른 리전으로 복사할 수 있습니다

Amazon EBS의 주요 특징
- 탄력적 크기 조정: EBS 볼륨은 필요에 따라 크기를 확장할 수 있습니다.
- 내구성: EBS는 데이터를 여러 가용 영역에 걸쳐 복제하여 높은 내구성을 보장합니다.
- 데이터보호: Amazon EBS 볼륨과 Amazon EBS 스냅샷을 암호화하여 데이터를 보호합니다. 암호화 작업은 EC2 인스턴스를 호스팅하는 서버에서 수행하므로, 인스턴스와 연결된 EBS 볼륨 뿐만아니라 EBS 스냅샷 모두 보안이 보장됩니다. 

#### 2.3.2 Instance Store
인스턴스 스토어는 EC2 인스턴스의 물리적 호스트에 직접 연결된 임시 스토리지로, EBS와 달리 인스턴스와 밀접하게 결합된 형태의 스토리지입니다. 인스턴스 스토어는 인스턴스가 종료되면 데이터가 삭제되기 때문에, 휘발성 스토리지로 간주됩니다. 인스턴스 스토어 볼륨의 수, 크기 및 유형은 인스턴스 유형에 따라 결정되고, 모든 EC2 인스턴스가 인스턴스 스토어 볼륨을 지원하지는 않습니다.

인스턴스 스토어의 주요 특징
- 고속 성능: 인스턴스 스토어는 인스턴스와 물리적으로 가까운 스토리지이므로 매우 빠른 I/O 성능을 제공합니다.
- 휘발성 데이터: 인스턴스가 중지되거나 종료되면 인스턴스 스토어에 있는 데이터는 사라집니다.
- 저장 용량 제한: 각 인스턴스 유형에 따라 제공되는 스토리지 용량이 다르며, 추가적으로 확장할 수 없습니다.

사용 사례
- 캐시 저장소: 데이터베이스의 캐시나 일시적으로 저장되는 임시 데이터에 적합합니다.
- 고성능 처리: 데이터 처리 속도가 중요한 애플리케이션에서 빠른 I/O가 필요한 경우에 사용됩니다.

#### 2.3.3 Amazon EFS (Elastic File System)

Amazon EFS는 완전 관리형 네트워크 파일 시스템으로, 여러 EC2 인스턴스에서 동시에 액세스할 수 있는 스토리지를 제공합니다. Linux 기반의 애플리케이션에서 주로 사용되며, 파일 시스템 크기를 자동으로 확장할 수 있습니다.

EFS의 주요 특징
- 동시 액세스: 여러 EC2 인스턴스에서 동시에 동일한 파일 시스템에 접근할 수 있습니다.
- 자동 확장: 데이터의 양에 따라 자동으로 크기가 조정되므로, 사용자는 스토리지 크기를 미리 정의할 필요가 없습니다.
- 내구성 및 가용성: 데이터를 여러 가용 영역(AZ)에 걸쳐 복제하므로 매우 높은 내구성과 가용성을 제공합니다.

사용 사례
- 공유 네트워크 파일 시스템: 여러 서버에서 동일한 파일 시스템을 공유해야 할 때.
- 웹 서버: 여러 웹 서버가 같은 파일에 동시에 접근해야 하는 경우(예: 웹 콘텐츠 호스팅).
- 컨테이너 및 서버리스 환경: 공유 파일 스토리지가 필요한 컨테이너 환경 또는 서버리스 애플리케이션에서 사용.

#### 2.3.4 Amazon S3 (Simple Storage Service)

Amazon S3는 객체 스토리지 서비스로, EC2와 함께 널리 사용되며, 정적 웹 콘텐츠, 백업, 로그 저장 등 대용량 데이터를 장기적으로 저장하는 데 적합합니다. S3는 높은 내구성과 확장성을 제공하며, 데이터 액세스 빈도에 따라 다양한 저장 클래스가 제공됩니다.

S3의 주요 특징
- 객체 스토리지: 파일이나 이미지 같은 데이터를 객체 단위로 저장하고, 각각의 객체는 고유의 키로 접근 가능합니다.
- 높은 내구성: S3는 99.999999999%의 내구성을 제공하며, 여러 리전 간 복제를 통해 데이터를 보호합니다.
- 저장 클래스: 자주 접근하는 데이터를 위한 S3 Standard부터, 거의 접근하지 않는 데이터를 위한 S3 Glacier까지 다양한 저장 클래스를 제공하여 비용을 절감할 수 있습니다.

사용 사례
- 백업 및 복구: 장기적으로 데이터 백업을 저장하는 용도.
- 정적 웹 콘텐츠 호스팅: 정적 파일(이미지, 동영상, HTML 파일 등)을 웹에서 호스팅.
- 로그 및 빅데이터 분석: 대량의 로그 데이터 저장 및 분석.

** 출처: https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/Storage.html


<a name="sg"></a> 
### 2.4 보안 그룹 
**보안 그룹(Security Group)**은 Amazon EC2 인스턴스 및 기타 AWS 리소스에 대한 가상 방화벽(Virtual Firewall) 역할을 하는 AWS의 보안 기능입니다. 보안 그룹은 인스턴스로 들어오는 인바운드 트래픽과 인스턴스에서 나가는 아웃바운드 트래픽을 제어합니다. 이를 통해 사용자는 허용된 IP 주소나 포트에서만 접근할 수 있도록 설정할 수 있으며, 외부의 불필요한 트래픽을 차단해 보안을 강화할 수 있습니다.

#### 2.4.1 보안 그룹의 주요 특징

1.	상태 저장(Stateful)
	- 보안 그룹은 상태 저장 방식으로 작동합니다. 즉, 인바운드 규칙에서 허용된 트래픽에 대해, 해당 트래픽이 아웃바운드 방향으로 나갈 때 별도의 규칙을 정의하지 않아도 자동으로 허용됩니다. 마찬가지로 아웃바운드 규칙에서 허용된 트래픽도 그에 대한 응답이 인바운드 방향으로 들어올 때 자동으로 허용됩니다.
2.	기본 허용 정책
	- 보안 그룹은 기본적으로 모든 트래픽을 차단하고, 사용자가 명시적으로 허용한 트래픽만 허용합니다. 이로 인해 불필요한 외부 접근을 최소화할 수 있습니다.
	- 인바운드 규칙을 정의하지 않으면 모든 인바운드 트래픽이 차단되며, 아웃바운드 규칙을 정의하지 않으면 모든 아웃바운드 트래픽이 차단됩니다.
3.	인바운드 및 아웃바운드 트래픽 필터링
	- 인바운드 규칙: EC2 인스턴스로 들어오는 트래픽을 제어합니다. 특정 IP 주소나 CIDR 블록에서 들어오는 트래픽만 허용할 수 있습니다.
	- 아웃바운드 규칙: EC2 인스턴스에서 나가는 트래픽을 제어합니다. 인스턴스에서 외부로 나가는 트래픽을 제한하거나 특정 대상에만 트래픽을 보내도록 설정할 수 있습니다.
4.	포트 및 프로토콜 제어
	- 보안 그룹은 특정 포트(예: 80, 443, 22)나 프로토콜(예: TCP, UDP, ICMP)에 대한 접근을 제어할 수 있습니다.
	- 예를 들어, SSH를 통해 EC2 인스턴스에 접근하려면 TCP 포트 22를 허용하는 규칙을 설정할 수 있습니다.
5.	연결된 여러 인스턴스에 적용 가능
	- 하나의 보안 그룹을 여러 EC2 인스턴스에 연결할 수 있습니다. 이를 통해 여러 인스턴스에 일관된 보안 정책을 적용할 수 있습니다.
6.	인스턴스의 실시간 적용
	- 보안 그룹은 실시간으로 적용되며, 사용자가 보안 그룹 규칙을 수정하면 즉시 EC2 인스턴스에 적용됩니다. 인스턴스를 다시 시작할 필요 없이 규칙이 변경된 상태로 바로 적용됩니다.
7.	보안 그룹의 제한
	- 하나의 EC2 인스턴스는 여러 보안 그룹에 속할 수 있으며, 보안 그룹마다 각각의 인바운드 및 아웃바운드 규칙을 가질 수 있습니다.
	- AWS에서는 보안 그룹당 규칙의 수에 제한이 있으며, 필요에 따라 늘릴 수 있습니다.

#### 2.4.2 보안 그룹 규칙 구성 요소

보안 그룹의 규칙은 트래픽을 허용할 대상, 포트 범위, 프로토콜 등을 명시하여 정의됩니다. 각 규칙은 다음과 같은 주요 구성 요소로 이루어집니다.

1.	IP Protocol (프로토콜)
	- 트래픽의 종류를 정의하는 프로토콜입니다. 주로 TCP, UDP, ICMP를 사용하며, 다른 프로토콜도 설정할 수 있습니다.
	- 예를 들어, 웹 트래픽은 TCP 프로토콜을 사용하며, 특정 네트워크 진단을 위해 ICMP(Ping)가 사용될 수 있습니다.
2.	Port Range (포트 범위)
	- 특정 트래픽을 허용하는 포트 번호 또는 포트 범위를 정의합니다.
	- 예시:
		- HTTP 웹 트래픽: TCP 80번 포트.
		- HTTPS 웹 트래픽: TCP 443번 포트.
		- SSH: TCP 22번 포트.
		- MySQL 데이터베이스: TCP 3306번 포트.
3.	Source / Destination (출발지 / 목적지)
	- 인바운드 규칙에서는 출발지(Source) IP 주소를 정의하고, 아웃바운드 규칙에서는 목적지(Destination) IP 주소를 정의합니다.
	- 특정 IP 주소, CIDR 블록(예: 192.168.1.0/24), 혹은 보안 그룹 자체를 출발지 또는 목적지로 설정할 수 있습니다.
	- IP 주소 범위 또는 다른 보안 그룹을 지정하여 허용된 트래픽의 출처를 세분화할 수 있습니다.
4.	Description (설명)
	- 규칙의 목적이나 이유를 설명하는 짧은 문구를 추가할 수 있습니다. 이는 사용자가 여러 보안 그룹을 관리할 때 규칙을 이해하는 데 도움을 줍니다.

#### 2.4.3 보안 그룹 설정 예시

1. SSH 접근을 허용하는 보안 그룹 설정
	- 인바운드 규칙:
		- 프로토콜: TCP
		- 포트: 22
		- 소스: 0.0.0.0/0 (모든 IP 주소에서 허용) 또는 특정 IP(예: 203.0.113.0/24)
	- 설명: 모든 곳에서 SSH 접근을 허용할 경우 모든 IP 주소(0.0.0.0/0)에서 트래픽을 허용하지만, 보안상의 이유로 특정 IP 주소나 범위만 허용하는 것이 일반적입니다.

2. 웹 서버(HTTP, HTTPS) 접근을 허용하는 보안 그룹 설정
	- 인바운드 규칙:
		- 프로토콜: TCP
		- 포트: 80 (HTTP) 및 443 (HTTPS)
		- 소스: 0.0.0.0/0 (모든 IP 주소에서 허용)
	- 설명: 웹 트래픽을 처리하기 위해 HTTP와 HTTPS 포트를 열어야 합니다.

3. MySQL 데이터베이스에 접근을 허용하는 보안 그룹 설정
	- 인바운드 규칙:
		- 프로토콜: TCP
		- 포트: 3306
		- 소스: 특정 보안 그룹 (예: 웹 서버 보안 그룹)
	- 설명: 데이터베이스는 외부에서 직접 접근되지 않도록 하고, 오직 특정 보안 그룹(예: 웹 서버 인스턴스)만 MySQL 포트에 접근할 수 있도록 제한합니다.

<a name="sg-practice"></a> 
#### 2.4.4 보안 그룹 설정 실습 
아래 테이블과 같은 프로토콜/포트 번호/소스을 허용하는 인바운드 규칙을 가지는 보안 그룹을 생성하시오.
	
|프로토콜|	포트 번호|소스|
|---|---|---|
|TCP	|22(SSH)|	0.0.0.0/0|
|TCP	| 80(HTTP)|	0.0.0.0/0|
	
	
1. https://console.aws.amazon.com/ec2/ 에서 Amazon EC2 콘솔을 엽니다.
2. 화면 상단 탐색 모음에서 보안 그룹을 생성할 **리전**을 선택
	- **보안 그룹은 리전에 고유**하므로 키 페어를 생성한 리전과 동일한 리전을 선택
	- 현재 위치와 관계없이 **리전**을 선택할 수 있음
3. 왼쪽 탐색 창에서 **네트워크 및 보안 > 보안 그룹 (Security Groups)**를 클릭
4. **보안 그룹 생성 (Create Security Group)**을 클릭
5. 새 보안 그룹의 이름과 설명을 입력
	- 기억하기 쉬운 이름을 선택
		- 예, 사용자이름_SG_리전이름  *Kwanu\_SG\_Seoul*
6. VPC 목록에서 default VPC 선택 (이미 선택되어 있음)
7. [**Inbound**] 탭에서 다음 규칙 생성
	1. **규칙 추가 (Add Rule)** 클릭
	2. **유형 (Type)** 목록에서 *HTTP*를 선택하고 **소스 (Source)** 목록에서 *Anywhere-IPv4* (0.0.0.0/0)를 선택
	1. **규칙 추가 (Add Rule)** 클릭
	2. **유형 (Type)** 목록에서 *SSH*를 선택, **소스 (Source)** 목록에서 *Anywhere-IPv4* (0.0.0.0/0)를 선택

8. **Create** 클릭

<a name="login"></a> 
### 2.5 EC2 인스턴스 로그인 정보 보호
AWS EC2 인스턴스는 기본적으로 SSH(리눅스) 또는 RDP(윈도우)를 통해 원격으로 접속할 수 있으며, 이를 위해 **키 페어(Key Pair)**를 사용한 인증 방식을 지원합니다. 키 페어는 프라이빗 키와 퍼블릭 키로 구성되며, 퍼블릭 키는 인스턴스에 저장되고 프라이빗 키는 사용자만 보유합니다.

![](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/images/ec2-key-pair.png)

- 프라이빗 키 보호:
	- 프라이빗 키는 절대로 공개되거나 타인과 공유되어서는 안 됩니다. SSH 로그인 시 프라이빗 키가 필요하며, 이를 안전하게 저장하고 관리하는 것이 중요합니다.
	- 프라이빗 키 파일(.pem 파일)은 시스템 내에서 적절한 권한 설정을 유지해야 합니다. 예를 들어, 리눅스에서는 프라이빗 키 파일의 권한을 다음과 같이 제한할 수 있습니다:

	```
	chmod 400 /path/to/your-key.pem
	```

- 퍼블릭 키: 
	- 퍼블릭 키는 EC2 인스턴스의 ~/.ssh/authorized_keys 파일에 저장되며, 이를 통해 인증을 진행합니다. 퍼블릭 키만으로는 로그인할 수 없으며, 반드시 프라이빗 키와 쌍으로 사용해야 합니다.

- 키 페어 생성:
	- AWS 콘솔, CLI 또는 SDK를 통해 새로운 키 페어를 생성할 수 있으며, 생성된 프라이빗 키는 최초 다운로드 시에만 제공되므로 안전한 장소에 백업하는 것이 중요합니다.

<a name="keypair-practice"></a> 
#### 2.5.1 키페어 생성 실습 
1. https://console.aws.amazon.com/ec2/ 에서 Amazon EC2 콘솔을 엽니다.
2. 화면 상단 탐색 모음에서 보안 그룹을 생성할 **리전**을 선택
	- **보안 그룹은 리전에 고유**하므로 키 페어를 생성한 리전과 동일한 리전을 선택
	- 현재 위치와 관계없이 **리전**을 선택할 수 있음
3. 왼쪽 탐색 창의 **네트워크 및 보안 > 키 페어** 선택
4. **키 페어 생성** 클릭
5. [**키 페어 생성**] 대화 상자의 [**이름**] 필드에 새 키 페어의 이름을 입력하고 **Create**를 클릭	
	- 기억하기 쉬운 이름 사용: *사용자이름/-key/-pair/-리전이름*
6. 브라우저에서 개인키(프라이빗 키) 파일이 자동으로 다운로드됨
	- 안전한 장소에 개인키 파일 ( 확장명이 *.pem*)을 저장
	- 사용자는 EC2 인스턴스를 새로 만들 때 **키 페어의 이름을 입력**하고, EC2 인스턴스에 연결할 때마다 해당하는 개인키를 입력해야 함

<a name="lifecycle"></a> 
### 2.6 인스턴스 수명주기
- **인스턴스 스토어** 기반 인스턴스는 **중지 후 시작을 할 수 없음**, 반면에 **EBS** 기반 인스턴스는 **중지 후 시작 가능**
- **running** 상태 있는 동안 비용 청구, (주의, 인스턴스에 접속하지 않아도 비용청구됨)
- **stopped** 상태에서는 인스턴스 사용 요금, 데이터 전송 요금은 부가되지 않으나, EBS 볼륨에 대한 요금은 부과됨
- 인스턴스 **재시작(중지 후 시작)**시, 
	- 이전 호스트 컴퓨터의 인스턴스 스토어 볼륨에 있는 데이터가 모두 손실
	- 모든 Amazon EBS 볼륨이 인스턴스에 연결된 상태로 유지되고 해당 데이터도 남아 있음
	- 프라이빗 IP 주소는 유지, **퍼블릭 IP는 변경됨** (EC2-VPC 인 경우)
- 인스턴스 **재부팅**시, 인스턴스가 동일 호스트 컴퓨터에서 유지되며 해당 퍼블릭/프라이빗 IP 주소 및 인스턴스 스토어 볼륨의 모든 데이터가 그대로 유지됨.

![](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/images/instance_lifecycle.png)

** 출처: https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/ec2-instance-lifecycle.html


<a name="ami"></a> 
### 2.7 아마존 머신 이미지 (AMI)
- **아마존 머신 이미지 (AMI)**는 필요한 소프트웨어 (예, 운영체제, 애플리케이션 서버, 애플리케이션)가 이미 구성되어 있는 템플릿
	- 하나의 AMI로 여러 인스턴스를 실행시킬 수 있음

	![](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/images/architecture_ami_instance.png)
	
- 공개된 다양한 AMI
	- AWS에서는 자주 사용되는 다양한 AMI를 공개하여 게시하고 있음
	- AWS 개발자 커뮤니티 회원들이 올린 자체 구성 AMI
	- 사용자 정의 AMI

- AMI 종류 :
	
	EC2 인스턴스의 루트 볼륨(주 부팅 디스크)이 어디에서부터 시작되는지를 정의하는 방식입니다. AWS에서는 두 가지 주요 루트 디바이스 유형을 지원합니다:

	- **인스턴스 스토어 기반 (Instance Store-Backed)** AMI
		- 인스턴스 스토어 기반 AMI를 사용하여 시작된 EC2 인스턴스는 해당 스토리지를 루트 디바이스로 사용합니다. 인스턴스 스토어(Instance Store)는 물리적 디스크에 저장되는 임시 스토리지로, 인스턴스가 종료되면 저장된 데이터는 사라집니다. 이러한 AMI는 주로 임시 데이터 처리가 필요한 워크로드나 캐싱 용도로 사용됩니다. 단점은 인스턴스가 중지되거나 종료될 때 모든 데이터가 삭제된다는 점입니다.
			
	- **아마존 EBS 기반 (Amazon EBS-Backed)** AMI
		- EBS 기반 AMI는 EC2 인스턴스가 EBS 볼륨을 루트 디바이스로 사용하게 설정된 AMI입니다. EBS는 네트워크에 연결된 블록 스토리지로, 인스턴스가 종료되더라도 데이터가 유지됩니다. EBS 볼륨은 독립적으로 존재하며, 인스턴스 종료 후에도 데이터를 계속 유지할 수 있어 영구 스토리지로 사용됩니다. EBS 볼륨을 스냅샷으로 백업하거나 다른 인스턴스에 다시 연결할 수 있어 유연성이 뛰어납니다. 장점은 인스턴스가 재시작되더라도 데이터가 유지된다는 것이며, 주로 장기 데이터 저장이나 중요한 애플리케이션에 사용됩니다.

	두 유형의 주요 차이점은 다음과 같습니다.
	- **데이터 유지 여부**: EBS 기반 AMI는 데이터가 인스턴스 종료 후에도 유지되지만, 인스턴스 스토어 기반 AMI는 인스턴스 종료 시 데이터가 손실됩니다.
	- **성능 및 비용**: 인스턴스 스토어는 로컬 디스크에 저장되므로 지연시간이 적을 수 있지만, EBS는 네트워크를 통해 액세스하므로 지연시간이 다소 발생할 수 있습니다. EBS는 별도 비용이 발생할 수 있습니다.


<a name="ec2-practice"></a> 
## 3. EC2 실습
### 3.1 [Amazon EC2 시작하기](https://docs.aws.amazon.com/ko_kr/AWSEC2/latest/UserGuide/EC2_GetStarted.html)

### 3.2 웹서버 실행
생성된 EC2 인스턴스에 연결된 상태에서 터미널에 다음 명령을 실행합니다.

1. 루트 권한을 획득합니다.

	```
	[ec2-user@ip-172-31-15-176 ~]$ sudo -s
	```
2. 리눅스의 패키지 관리 시스템인 YUM을 사용하여 Apache HTTP Server(일반적으로 httpd로 불림)를 설치합니다.

	```
	[ec2-user@ip-172-31-15-176 ~]$ yum install httpd -y
	```
3. 웹서버를 시작시킵니다.

	```
	[ec2-user@ip-172-31-15-176 ~]$ service httpd start
	```

4. EC2 인스턴스의 퍼블릭 IPv4 주소 및 퍼블릭 IPv4 DNS 를 웹브라우저 주소창에 입력 후 엔터를 쳐서, 웹 서버가 동작하는지 확인합니다.

### 3.3 index.html 문서 작성하여 실행하기
- index.html은 웹서버의 기본 웹 페이지 역할을 담당하는 파일입니다.
- Apache 서버는 기본적으로 /var/www/html 디렉토리에서 웹서버 관련 파일을 제공하도록 설정해야 합니다. 따라서 index.html 파일은 이 디렉토리에 위치해야 합니다.
1. /var/www/html/ 하위에 index.html을 생성합니다.

	```
	[ec2-user@ip-172-31-15-176 ~]$ nano /var/www/html/index.html
	```
2. nano 에디터 창으로 전환되면, 다음 문장을 입력합니다.

	```
	Hello, This is my First Web page
	```
3. [CONTROL]-x를 누른 후, "Save modified buffer?" 메시지가 나타나면 *y*를 입력 후 엔터를 입력하여 저장 후 에디터 창을 빠져나옵니다. 

4. 앞서 수행했던 EC2 인스턴스의 퍼블릭 IPv4 주소의 웹 브라우저를 새로고침하여 결과를 확인합니다.

### 3.4 EC2 인스턴스 이미지(Amazon Machine Image, AMI) 생성하기
1. **EC2 인스턴스 선택**: AWS Management Console에 접속하여 EC2 대시보드에서 현재 실행 중인 인스턴스 목록을 봅니다. 그 중 AMI로 만들고자 하는 인스턴스를 선택합니다.

2. **이미지 생성**:
	- 선택한 인스턴스의 '작업(Actions)' 버튼을 클릭합니다.
	- '이미지 및 템플릿' → '이미지 생성(Create Image)'를 선택합니다.

3. **이미지 설정**: 이미지 생성 대화 상자에서 다음과 같은 설정을 입력합니다.
	- 이미지 이름(Image Name): 생성될 AMI의 이름을 지정합니다.
	- 이미지 설명(Image Description): 이미지에 대한 설명을 적어 추가 정보를 입력할 수 있습니다.
	- 볼륨 설정(Block Device Mapping): 볼륨 크기나 유형을 필요에 따라 수정할 수 있습니다.

4. **이미지 생성 완료**: 설정을 완료하고 'Create Image'를 클릭하면 이미지 생성 작업이 시작됩니다. 이미지 생성은 몇 분이 걸릴 수 있습니다. AMI는 EC2 대시보드의 '이미지' → 'AMI' 섹션에서 확인할 수 있습니다.

### 3.4. AMI를 이용하여 새로운 EC2 인스턴스 시작하기
1. **AMI 선택**: EC2 대시보드에서 '이미지' → 'AMI' 섹션으로 이동하여 생성한 AMI를 찾습니다.

2. **인스턴스 시작**: 생성된 AMI를 선택한 후, 'Launch' 버튼을 클릭합니다.

3. **인스턴스 구성**:
	- 인스턴스 유형(Instance Type) 선택: 사용할 인스턴스 유형을 선택합니다. 이 유형은 CPU, 메모리, 네트워크 성능 등에 따라 달라집니다.
	- 인스턴스 세부 사항 구성(Configure Instance Details): 인스턴스 개수, 네트워크 설정, IAM 역할 등 세부 사항을 설정합니다.
	- 스토리지 추가(Add Storage): 볼륨 크기 및 유형을 설정합니다.
	- 태그 추가(Add Tags): 인스턴스에 태그를 추가할 수 있습니다.
	- 보안 그룹 설정(Configure Security Group): 기존 보안 그룹을 선택하거나 새로운 보안 그룹을 생성하여 인스턴스에 대한 네트워크 접근을 관리합니다.
4. **키 페어 설정**: 기존 키 페어를 사용하거나 새 키 페어를 생성하여 인스턴스에 SSH로 접속할 수 있도록 설정합니다.

5. **인스턴스 시작**: 모든 설정이 완료되면 'Launch' 버튼을 클릭하여 인스턴스를 시작합니다.

6. **인스턴스 상태 확인**
	- 새로 시작된 인스턴스는 'Running' 상태가 될 때까지 몇 분 정도 걸릴 수 있습니다. EC2 대시보드의 '인스턴스' 섹션에서 새로운 인스턴스가 정상적으로 시작되었는지 확인할 수 있습니다.

이 과정을 통해 기존 인스턴스의 상태를 그대로 유지한 새로운 EC2 인스턴스를 빠르게 생성할 수 있습니다.